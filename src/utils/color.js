const inactiveColor = '#191b20'
const correctColor = '#538d4e'
const incorrectColor = '#303030'
const initialColor = 'red'
const finalColor = 'orange'
const toneColor = 'purple'

const color = {
    inactive: inactiveColor,
    correct: correctColor,
    incorrect: incorrectColor,
    initial: initialColor,
    final: finalColor,
    tone: toneColor,
}

export default color